[package]
name = "pam_password_fingerprint"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
zbus = { version = "4" }
tokio = { version = "1", features = ["rt", "time"] }
futures-util = "0.3"
